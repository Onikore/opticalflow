# Backlog экспериментов

Очередь идей (от пользователя + свои). Категории по выполнимости на текущих данных
(кэш реального полёта `data/bag_flight/`, синтетика, OpenCV) без нового железа.

## Готово (закрыто, см. FINDINGS.md)

- median, parabolic, pyramid(zero-mean coarse), boundary_reject, MAD-фильтр — приняты
- census, uniqueness, forward-backward, peak_quality — опциональные флаги
- Плотность сетки — плато; взвешивание по кривизне — отклонено (ломает выбросы)
- Гиро-де-ротация, DroneCAN-выход, quality на слепых сценах, карта диапазона — стенды
- Разбор реального полёта (rosbag) + кэш

## Выполнимо сейчас (софт + имеющиеся данные)

Приоритет по ценности×простоте:

1. **Сравнение с DIS-Flow и LK** (OpenCV `DISOpticalFlow`, `calcOpticalFlowPyrLK`).
   Прогнать на баге, сравнить траектории/resid. Позиционирует наш лёгкий блок-матчер
   против стандартов. Быстро, информативно. → **тастер сделан, см. results/bag_vs_optflow.png**

2. **RANSAC для global motion**: вместо медианы фитить глобальную трансляцию по блокам через
   RANSAC. → **ПРОВЕДЕНО, отклонён.** Шум −57% (0.56→0.24, находит плотный консенсус), НО
   яркость +38% (1.5→2.1): при дрейфе gain блоки разъезжаются, узкий tau цепляет неверный
   кластер. MAD даёт бóльшую часть выигрыша по шуму без поломки яркости. clean/outlier ≈.

3. **Пространственная карта надёжности (spatial reliability map)**: → **ПРОВЕДЕНО**
   (`results/spatial_reliability.png`). Гипотеза «края хуже» подтверждена ЧАСТИЧНО, механизм
   ДРУГОЙ: плохая только верхняя полоса кадра (валидных 0.68 vs 0.90), симметричные края ок.
   Острота пика по краям НЕ хуже (431 центр vs 441 края) → **виньетирования/дисторсии нет**.
   Причина — конструкция дрона в верху кадра (нога/луч ловит не землю). Фикс: маска верхней
   зоны под КОНКРЕТНЫЙ монтаж, а не универсальное центр-взвешивание. Actionable, но
   mount-specific.

4. **Temporal filtering** (EMA): → **ПРОВЕДЕНО, отклонён.** Чуть чистит путь
   (2.01→1.76 при α=0.3), но точность НЕ растёт (resid 1.06→1.09, лаг размывает движение).
   Путь остаётся ~2× даже при сильном сглаживании → инфляция это низкочастотный дрейф
   зависания, не джиттер. EKF сам фильтр — префильтрация избыточна и вредна (лаг).

5. **Temporal drift analysis**: → **ПРОВЕДЕНО** (`results/temporal_drift.png`). Ключ: **дрейф
   НЕ накапливается** за 155 с open-loop — скорость роста ошибки ≈0 (зависание −0.002 м/с,
   движение −0.004 м/с). Ошибка ограничена ~1–3 м, финальная 0.72 < средней 0.90. Разрешает
   загадку «путь 2×»: это локальные петли, которые уходят и ВОЗВРАЩАЮТСЯ, а не накопление.
   Позиция точна несмотря на «грязный» путь → 2× косметика, не проблема точности.

6. **Размер блока (TILE_SIZE)**: → **ПРОВЕДЕНО, условно-положительный.** T=16 бьёт шум −36%
   (0.56→0.36) и яркость −47% (1.5→0.79) — больше пикселей усредняется. clean/outlier ≈. НО
   цена TILE²: T=16 = 4× SAD, T=12 = 2.25× (горячий цикл STM32). Реальная ручка
   точность/вычисления: T=8 дефолт по бюджету, T=12/16 если есть запас. Для адаптива — под
   железо. (Требует параметризации хардкоженных 8 в SAD-функциях, если внедрять.)

7. **Weighted SAD**: → **ПРОВЕДЕНО, отклонён.** Гауссов центр-вес хуже везде (теряет инфу
   с краёв блока). Градиент-вес помогает только яркости (−42%), но портит clean/noise/outlier
   — меняет одно на многое. Равномерный SAD правильнее; яркость лучше через census/zero-mean.

8. **Self-supervised калибровка FOV**: → **ПРОВЕДЕНО, ограничено (подтвердилось).** resid
   плоский (1.058) при любом focal — Procrustes scale-инвариантен, по форме не калибруется.
   По масштабу scale=1 при focal≈118px (FOV 44°) vs паспортный 110px (47°), ~7%. Но эталон
   сам OF-ведомый → это подгонка нашего OF к ИХ OF, не к реальности. Для абсолютного FOV
   нужен независимый RTK-GPS (в баге нет). Работает как относительная сверка, не абсолютная.

## Математические метрики совпадения (`results/match_metrics.png`)

- **Фазовая корреляция (FFT)**: → **ПРОВЕДЕНО, не для per-block.** Глобальная (весь кадр)
  сильна на шуме (0.56→0.23) и яркости (1.5→0.28) — инвариантна к яркости (фаза) и
  усредняет в частотной области. НО outlier катастрофа (2.2) — глобальная оценка не
  отбраковывает выбросы. Блочная (32×32) не взлетела (мало блоков). Как RANSAC: хороша на
  шуме/яркости, слепа к выбросам. + FFT-стоимость.
- **ZNCC (zero-mean normalized cross-corr)**: → **ПРОВЕДЕНО, ниша.** Полная инвариантность к
  offset+gain. **Лучший на clean (0.208→0.164, −21%!), bright (−89%), outlier (−18%)** —
  золотой стандарт метрики, точнее на чистом сигнале. НО шум +85% (нормировка на std раздувает
  шум в низкоконтрастных блоках), на bright+шум high-pass бьёт (0.50 vs 1.35). Дороже (умнож.+
  норм.). Вывод: ZNCC — если мало шума и есть вычисления; high-pass — для реального шума.
- Общий вывод: все яркостно-инвариантные методы (census, high-pass, ZNCC, фаза) фундаментально
  усиливают шум (убирают НЧ → ВЧ-шум растёт). Бесплатной яркостной инвариантности нет; лучший
  путь — фикс экспозиции в камере.
- **Тензор структуры (Shi-Tomasi λ_min)**: → **ПРОВЕДЕНО, нишевый детектор aperture.** λ_min из
  градиентов img1 (до матчинга) = насколько поток определён. Ловит **полосы/грани** (stripes
  q=100→0 при λ>500), но НЕ плитку (периодика: градиенты в обе стороны → высокий λ_min).
  Вскрыло: два разных механизма aperture — **грани ловит тензор структуры, периодику ловит
  uniqueness (peak-ratio)**, комплементарны. На нормальных сценах точность не меняет (блоки уже
  фильтрованы по текстурности). НЕ провалился как взвешивание по кривизне (меряет текстуру, не
  совпадение → окклюдер не обманет). Опция для сцен с линейными структурами (дороги/борозды).

## Прочие проведённые

- **Pixel-locking коррекция субпикселя** (`use_subpix_correction`): → **ПРОВЕДЕНО, ВАЖНЫЙ
  УРОК.** Из литературы: SAD+парабола систематически тянет субпиксель к целым (bias 0.13px,
  истина 0.3→оценка 0.1, V-образная SAD не подходит параболе). Замерил кривую, построил
  корректор (LUT-инверсия). **Синтетика: clean −72%, outlier −70%** (субпиксельный bias был
  доминирующей ошибкой в чистых условиях!). НО **на реальном баге ХУЖЕ: resid 1.27→1.64
  (+29%)**. Причина: LUT калиброван на scipy bilinear-сдвиге = **переобучение на артефакт
  тест-генератора**; реальная камера имеет другую субпиксельную статистику + шум/смаз, и
  синтетический корректор усиливает ошибки. Методологический вывод: **синтетический выигрыш
  ОБЯЗАН проверяться на реальных данных** — иначе это фитинг под генератор. Флаг оставлен для
  ре-калибровки LUT на реальной камере (техника реальна, кривая — нет). SSD вместо SAD даёт
  меньший bias (0.104 vs 0.129) без усиления шума, но дороже.

- **Illumination pre-normalization (high-pass)**: → **ПРОВЕДЕНО, ПРИНЯТО как опция**
  (`use_highpass`). Вычесть локальное среднее (бокс-фильтр) перед SAD -> яркостная
  инвариантность. Тот же фундаментальный размен что census (яркость↓ ценой шума↑, т.к. НЧ
  убирается -> ВЧ-шум усиливается), НО мягкое ядро (k=13) даёт яркость −86% при штрафе шума
  всего +14% (census +83%). Работает на реальном баге (resid 1.06→1.05, scale 0.937→0.949,
  valid 1641), где census ломался (66 валидных). Дешевле (сепарабельный бокс). **Заменяет
  census** как основная опция для автоэкспозиции. LCN (нормировка контраста) — хуже (шум ×2).

## Батч от пользователя (метрики, регуляризация, темпоральные, поиск)

Уже сделано ранее из этого списка: NCC/ZNCC (ниша低-шума), Structure Tensor (детектор
aperture по граням), Forward-Backward (`use_fb_check`), Cross-check Pyramid Level 0
(= наша пирамида: грубо + ±2 финиш).

- **Gradient-based SAD** (SAD по модулю градиента): → **ПРОВЕДЕНО, отклонён.** Яркость чинит
  (0.247, как high-pass), но шум ХУЖЕ high-pass (1.02 vs 0.64): Sobel-производная усиливает
  ВЧ-шум сильнее мягкого бокс-фильтра. Доминируется high-pass. Подтверждает: производная =
  самый агрессивный high-pass = максимальный шум-налог.

- **Триангулярная (V) субпиксельная интерполяция** (`use_tri_subpix`): → **ПРОВЕДЕНО,
  ПРИНЯТО.** Алгоритмический фикс pixel-locking без LUT. SAD (L1) даёт V-образный минимум,
  а парабола предполагает U → систематический bias. V-интерполяция правильна для SAD:
  RMS bias 0.144→**0.088 px (−39%)**. На бенчмарке clean 0.208→0.127 (−38%), outlier −36%,
  noise/bright нейтрально. **На баге НЕ вредит** (resid 1.96→1.95, scale чуть лучше) — в
  отличие от LUT-коррекции (+29%): принципиальный оценщик переносится на реальность, фит под
  синтетику — нет. Gauss (лог-парабола) хуже (0.177, предполагает ещё острее пик). Заменяет
  параболу как рекомендуемый субпиксель.

- **Vector Median Filter (VMF)** — пространственная отбраковка: → **ПРОВЕДЕНО, синтетика
  соврала.** Локальная медиана векторов + фикс.порог (0.8px) отбрасывает разброс. Синтетика:
  noise 0.56→0.21, bright 1.5→0.18 (резко бьёт MAD, т.к. MAD относительный k·MAD велик когда
  всё поле разъехалось, а VMF фикс.порог режет независимо). НО **на баге ХУЖЕ** median+MAD
  (1.09→1.16, порог не важен): синт. bright/noise — стресс-уровень, на баге между кадрами
  (125мс) коррупция мягче, а реальные rolling shutter/дисторсия дают легит. вариацию, которую
  фикс.порог срезает. Яркость лучше лечит high-pass (переносится). Не принят — 2-й случай, где
  синт≠реал.

- **Bilateral фильтрация поля потока**: взвешенный VMF (вес по разнице векторов/quality). →
  **ПРОПУЩЕНО как subsumed**: VMF (жёсткая версия) не перенёсся на баг; взвешенная почти
  наверняка так же (для глобального движения нет границ движения, что bilateral бережёт).
- **3D cost volume (темпоральный матчинг 3 кадра)**: → **ПРОВЕДЕНО, ПОЛОЖИТЕЛЬНО (переносится!).**
  Константная скорость v: `cost(v)=SAD(a,b@v)+SAD(b,c@2v)`, минимум по v через 2 интервала.
  **На реальном баге resid 1.94→1.76 (−9%)** — один из немногих, кто помог на РЕАЛЬНЫХ данных
  (усредняет покадровую коррупцию шум/блюр). Косты: 2× SAD, 3 буфера кадров, ограниченный
  диапазон (2v в кадре), **допущение const-vel ломается на манёврах** (где нужна надёжность),
  scale чуть уехал (1.09→1.18). Перспективно и условно (гладкий полёт + бюджет). Не внедрён в
  модуль (нужна 3-кадровая буферизация + обработка манёвров), но валидирован.

- **Temporal bilateral фильтр векторов** (edge-preserving по времени): → **ПРОВЕДЕНО,
  ПОЛОЖИТЕЛЬНО (переносится).** Вес прошлых кадров ∝ `exp(-|Δv|²/2σ²)` → сглаживает шум на
  гладких участках, но при манёвре (большая разница векторов) вес падает → без лага. **На баге
  resid 1.09→1.02 (−6%)**, путь не раздут (2.04→2.03) — в отличие от EMA (1.10, хуже + лаг).
  Интеграционный слой (пост-фильтр потока, не блок-матчинг), дёшев. Оговорка: EKF сам фильтрует
  → польза частично дублируется, но как сенсорный пост-фильтр честно лучше EMA.

- **Search strategy (spiral + early-termination, adaptive window)**: → **ПРОВЕДЕНО,
  не для точности.** Spiral+early-term: 60% экономии SAD-операций, НО точность рушится
  (clean 0.40→1.38) — ранняя остановка берёт первый «достаточный» минимум, не глобальный.
  Безопасный порог почти не срабатывает. Для STM32 лучший путь скорости — **CMSIS SIMD**
  (`__USADA8` = 4 SAD/такт, 4× без потери точности), а не spiral. Пирамида УЖЕ сокращает
  поиск (= «cross-check pyramid level 0» из списка). Adaptive window экономит на слаботекстур.
  блоках (и так бракуются). Оптимизации — на этап C-порта, через SIMD.

## Валидация сигналов для EKF

- **Quality → реальная ошибка** (`results/quality_vs_error.png`): → **ПРОВЕДЕНО, quality
  валиден.** 500 сэмплов с варьируемым шумом/блюром/яркостью/скоростью. Корреляция
  quality↔ошибка **−0.578**, ошибка монотонно падает: q≈125→3.4px, q≈220→1.4px, q≈250→0.9px.
  EKF-взвешивание по quality **оправдано**. НО предсказатель несовершенный (разброс внутри
  бина большой, std≈среднему при q=250): наш quality чисто по meancount (доля валидных
  блоков), не учитывает остроту пика/MAD-разброс. Улучшить — вложить peak-sharpness
  (`peak_quality` частично делает) + разброс векторов после MAD в итоговый quality.
- **Spread-aware quality**: → **ПРОВЕДЕНО, не принят.** Сырой разброс векторов после MAD
  коррелирует с ошибкой хорошо (+0.66). НО в **полном пайплайне** meancount уже коррелирует
  −0.50 (прежний −0.06 был артефактом теста — count до MAD без boundary). Spread-quality как
  сигнал (`255·(1−spread/ref)`) дал −0.45 — не бьёт meancount. Флаг убран. Meancount уже
  приличный предиктор в полном пайплайне; отдельный spread-сигнал сам не улучшает.

- **Порог чувствительности** (минимальная детектируемая скорость): → **ПРОВЕДЕНО.** Фикс.сцена,
  варьируется только шум. **Точность (шумовой пол) отличная**: std потока 0.017 px (σ=2) —
  0.06 px (σ=4) = **~1.6 см/с на h=2м, ~0.4 см/с на h=0.5м**, сенсор очень повторяем. НО
  **систематическая недооценка малых скоростей**: v=0.2→bias −0.12, v=0.1→−0.08, v=0.05→−0.06
  (сжатие к нулю, остаточный pixel-locking под шумом, макс у ~0.2px как S-кривая). SNR>1 до
  ~0.05 px/кадр. Итог: детектирует движение до ~0.05 px/кадр, но медленный крип занижает на
  ~0.05–0.12 px (до ~5 см/с на h=2м) — определяет нижнюю границу, связано с дрейфом зависания.

- **Rolling shutter компенсация** (приоритет-3 плана, последний непроверенный пункт fallback.md):
  → **ПРОВЕДЕНО, RS минорен для потока.** Симуляция RS-захвата (построчная задержка Tread) во
  время вращения+трансляции. RS-эффект = |поток_RS − поток_GS|: **постоянное вращение 60°/с →
  0.07px** (shear одинаков в обоих кадрах → СОКРАЩАЕТСЯ во frame-to-frame). Кусается только при
  угловом **ускорении**: 40→80°/с = 0.15px, 20→60°/с = 0.54px (разный shear в кадрах). При
  высоких ω измерение падает за диапазон — но это де-ротация/пирамида, не RS. Вывод: полная
  построчная коррекция ИЗБЫТОЧНА для потока (в отличие от одиночной геометрии/ститчинга); хватило
  бы гейта quality по угловому ускорению. Доминирует range/де-ротация (решено пирамидой).

## Де-риск C-порта

- **Fixed-point эмуляция**: → **ПРОВЕДЕНО, порт де-рискован.** Q8-субпиксель (1/256 px,
  целочисленный: `128*(lo-hi)//den`) + int SAD **практически идентичны float**: макс
  расхождение **0.0011 px** на всех сценариях. Весь алгоритм переносится в int-арифметику
  STM32 без потери точности. Диапазоны против переполнений: SAD 8×8 max=16320 (uint16 ок,
  int32 безопаснее); субпиксель Q8 числитель до 2.09M → **int32 обязателен**; high-pass
  бокс 13×13 = 43095 → int32. Медиану/MAD тоже можно в Q8. CMSIS `__USADA8` даёт int SAD
  напрямую.

- **Гиро-центрирование поиска** (inertial-aided): → **ПРОВЕДЕНО, не помогает здесь.**
  Целиком (центр поиска = гиро-предсказание) ХУЖЕ пирамиды (1.09→1.38): гиро шумит/биас,
  центрирование корчит 96% НЕвращающихся кадров. Гибрид (гиро только когда вращение >порога):
  на всём полёте **только 1/1669 кадров** вылетает за потолок пирамиды (9.5px) → чинить нечего,
  гибрид 1.14 (чуть хуже 1.09). Ключ: смешаны 2 вещи — центрирование лечит ВЫХОД ЗА ДИАПАЗОН
  (пирамида уже покрыла), а петли-на-наклоне это ДЕ-РОТАЦИЯ (вращат. поток интегрируется как
  трансляция), лечится вычитанием гиро после измерения (не наведением поиска). Де-ротация же
  глобально маргинальна (вращение эпизодично). **Пирамида сделала гиро-центрирование
  избыточным**; гиро-аиднг проявится только на устойчивом резком вращении.

- **Lever-arm смещения камеры** (камера не в центре дрона): → **ПРОВЕДЕНО, коррекция валидна,
  на баге пренебрежима.** Смещённая камера при вращении получает добавочную скорость `ω×r`;
  EKF нужна скорость центра: `v_центр = v_камеры − ω×r`. Компоненты: горизонт. смещение (rx,ry)
  × рыскание → `ωz·(−ry,rx)`; вертик. rz × наклон → `(ωy·rz, −ωx·rz)`. **На баге эффект ~0**
  (даже r=0.4м: resid 1.30→1.30) — дрон летел гладко (медиана рыскания 1°/с, наклона 3°/с).
  Важно при вращении + низкой высоте: паразит в потоке `ω·r/h`, при ωz=1рад/с,r=0.2м,h=0.5м =
  0.4 рад/с (огромно), на h=13м = 0.015 (ничто). Дёшево (векторное произв. с гиро) → внести в
  интеграционный слой (рядом с де-ротацией) как бесплатную страховку от резких манёвров у земли.

## Мульти-сенсор (избыточность)

- **Мульти-IMU для де-ротации (синт, реальная гиро-трасса)**: → **ПРОВЕДЕНО**
  (`results/multi_imu.png`). Гироскоп используется для де-ротации (вычесть `f·ω·dt`); качество
  упирается в шум/биас/сбои гиро. Monte-Carlo 300 реализаций, ICM-42688 уровень (шум 0.06°/с,
  биас между IMU 0.15°/с):
  - **√N снижение**: дрейф де-ротации 1 IMU=5.9м → 2 IMU=4.3 → 3=3.4 → 4=2.8м (строго 1/√N).
    4 IMU почти вдвое режут дрейф.
  - **Отказ критичен**: 1 сбойная IMU из 3 при наивном СРЕДНЕМ = 20м дрейфа (ХУЖЕ одной
    исправной!). МЕДИАНА-голосование отбрасывает сбой → 5.2м. Вывод: несколько IMU + робастная
    медиана, НЕ среднее. 3 IMU = минимум для голосования (выкинуть 1 сбой).
  - Честно: абсолютный дрейф bias-доминирован, реальный EKF оценивает биас гиро онлайн →
    меньше. Но √N и отказоустойчивость реальны. Common-mode вибрацию усреднение НЕ убирает
    (все IMU на жёстком монтаже видят её одинаково) — нужна мех. развязка + фильтр.
  - IMU дёшевы/крошечные (ICM-42688 ~$), низкий счёт → 3 IMU = дешёвый выигрыш надёжности.

## Нужны доп. данные / железо

- **Multi-camera fusion (видимый + ИК)**: → **ПРОВЕДЕНО, ПОЛОЖИТЕЛЬНО**
  (`results/multicam_fusion.png`). `/cam_usb` = видимый (= `/odometry/image`, VO-камера),
  `/cam_usb0` = ИК, обе вниз, перекрывающийся обзор, одинаковые интринсики. Прогон нашего
  потока на обеих + взвешивание по quality: vis 1.33 м, ИК 2.05 м (хуже — меньше текстуры),
  **fused 1.19 м (−11% vs vis)** + чуть выше availability. ИК душится где плох, но добавляет
  комплементарный сигнал где видимая слаба. Кэш `data/bag_flight/multicam.npz`.
  Дальше: fore-aft база даёт скорость по временной задержке (не пробовано).

- **Массив 3-4 downward-камеры (синт)**: → **ПРОВЕДЕНО** (`results/array_yaw.png`). Массив
  разнесённых камер восстанавливает **yaw БЕЗ гироскопа** через аффинный фит поля потока
  `f_k = t + M·r_k` (differencing изолирует вращение от трансляции). Результаты:
  - **4 на лучах** (база 0.35м, h=2м): ошибка yaw **4°** — точно (45→45, 90→89, 150→151).
  - **3 треугольник** (база 0.1м): ошибка **11°** — занижает, малая база = слабый сигнал.
  - **высота бьёт**: 4-луча на h=8м → ошибка 13° (сигнал ∝ b/h, на высоте тонет в шуме).
  - трансляция восстанавливается точно (√N усреднение): v=(1.0,-0.5)→(0.94,-0.49).
  ЧЕСТНО: массив даёт только **yaw** (curl поля). Pitch/roll — равномерный сдвиг образа,
  сокращается в differencing → неотличим от трансляции, нужен гиро или омни. И yaw слабеет
  на высоте. Вывод: массив = робастность/избыточность (√N) + yaw-подмога/дубль гиро на
  малой высоте с БОЛЬШОЙ базой (лучи>>треугольник), НЕ полная замена IMU. Лучшая конфиг для
  позиционирования: 4 на лучах + IMU (гиро для pitch/roll и высоты, массив для yaw+шума).
- **Self-supervised cam-IMU offset + temporal delay**: минимизация ошибки EKF2. Нужен
  настоящий независимый эталон (RTK-GPS) — в текущем баге его нет (OF инжектится как GPS).
- **Rolling shutter компенсация** (приоритет-3 плана): нужен реальный RS-сенсор + синхро-IMU.

- **FullHD + дальномер 200 м (сим. цифровых кропов)**: один FullHD-сенсор (объектив ~60°,
  f_native≈1663 px) даёт МНОГОМАСШТАБНЫЙ стек цифровых кропов без второй камеры:
  wide=даунскейл всего кадра (f=83), mid=кроп 384→96 (f=416, ~13°), narrow=нативный кроп 96
  (f=1663, ~3.3°). Шум скорости на зависании (м/с, кадр @30fps): narrow 0.007@5м →
  0.045@100м → **0.120@200м** (после EKF-усреднения ~2–4 см/с — удержание на 200 м реально);
  mid 0.72@200м; wide мёртв >100м. НОВОЕ узкое место — вращение: 10°/с на narrow = 9.7 px
  ≈ потолок пирамиды → **гиро-центрирование окна поиска становится ОБЯЗАТЕЛЬНЫМ** (на f=110
  было отвергнуто как избыточное — контекст изменился; шум гиро 0.06°/с → 0.06 px, наведение
  точное). Плюс: короткая экспозиция (motion blur ×15), виброразвязка ×15 важнее, текстура
  GSD 12 см @200 м — сцено-зависимо. Итог: камера FullHD + лидар 200 м + RPi = рабочий
  эшелон ~1–200 м с одним сенсором.

- **Визуальная ре-локализация (teach-and-repeat, прототип)**: на баге-1 (замкнутый полёт)
  проверено узнавание мест: пары кадров Δt>60с, Δpos<4м, матчинг фазовой корреляцией с
  яу-компенсацией (поворот кадра на Δyaw из IMU). Результат на КРОШЕЧНЫХ wide-кадрах 96px
  (GSD 12см@13м): **60% уверенных матчей (resp>0.1), медиана ошибки 1.62 м**, p90 4.2м.
  Вывод: «визуальные хлебные крошки» работают на нашем стеке уже сейчас; с mid-кропом
  256px FullHD (GSD 24см@100м, захват ±11м) ожидается суб-метровая коррекция. Основа
  системы RTH по пути без GPS: дрейф одометрии (~1%) обнуляется на каждой крошке.

- **Map-matching слой (`src/map_localize.py`, отдельный модуль)**: абсолютная локализация по
  геопривязанной подложке для миссий в WGS84 (QGC/MP) без ГНСС. Демо на баге-1: мозаика из
  первых 60% полёта = «спутниковая подложка», кадры возврата локализуются с прайором ±6 м
  (имитация дрейфа): **65% уверенных фиксов, медиана 0.41 м, p90 2.1 м** (после peak-dominance
  гейта против самоподобной травы; было p90 5.2). Self-test 0.8 м ≈ лоскутность самой мозаики.
  Пайплайн: кадр → север-вверх (yaw) → масштаб карты (h/f/GSD) → TM_CCOEFF_NORMED вокруг
  прайора → (x,y)+conf → enu_to_wgs84 → GPS_INPUT (тот же путь, каким OF уже инжектится).
  Реальное применение: подложка = ортофотоплан 0.1–0.6 м/px; порог conf перекалибровать.
- **Спутниковая подложка (репетиция кросс-сенсора)**: `GeoMap.from_image()` + high-pass
  нормализация обеих сторон. Тест: мозаика деградирована до «спутника» (0.5 м/px, гамма/
  яркость/блюр), живые кадры локализуются: **31% уверенных, медиана 3.6 м** (vs 65%/0.41 м
  на своей мозаике). ВАЖНО: репетиция на h=13 м пессимистична — шаблон кадра на 0.5 м/px
  всего ~24 px; на целевых 50–100 м кадр покрывает 43–87 м → шаблон 86–174 px → матчинг
  кратно надёжнее. Для реального теста нужен фрагмент снимка района бага (~56.83257N
  60.79318E) с координатами углов.
- **Инструменты спутникового теста**: `src/fetch_sat.py` (тайлы google/esri/bing → PNG+мета
  с ENU-привязкой; Яндекс исключён — эллиптический Меркатор; математика проверена dry-run:
  round-trip 1e-14°, GSD z19=0.163 м/px на 56.8°) и `src/sat_test.py` (3 стадии:
  авто-калибровка конвенции осей 8 вариантов + верификация топ-2 узким поиском; оценка
  сдвига баг↔тайл; основной прогон). Репетиция на деградированной мозаике со сдвигом
  привязки (+4,−3): конвенция выбрана верно, сдвиг восстановлен (+4.3,−2.6), 28% фиксов,
  медиана 3.45 м (h=13, пессимистично). Готово к реальному снимку.
- **РЕАЛЬНЫЙ кросс-датовый тест против спутника Google/ESRI** (закрыто последнее допущение
  бюджета миссии!): песочница смогла качать тайлы; `fetch_sat` скачал район багов (z19,
  0.163 м/px, 361 тайл), `sat_test` прогнал оба бага. **Google: баг-1 42% фиксов,
  медиана 0.49 м, p90 1.6 м; баг-2 42%, медиана 0.27 м, p90 3.0 м.** ESRI (снимок другой
  даты): 17%, медиана 1.14 м — провайдер имеет значение, Google для этого района лучший.
  Межпровайдерский сдвиг привязки Google↔ESRI ≈ 4 м (подтверждает трюк «миссия на той же
  подложке»). Конвенция осей выбирается автоматически per-flight (yaw-ноль различается между
  полётами: flipv vs flipv+180), сдвиг баг↔тайл ~(+28..+35, +21..+27) м оценивается стадией 1.
  Сцена с якорями (здание/парковка/дороги); чистый лес будет хуже — как и бюджетировалось.
  Тайлы не в git (data/sat/ в .gitignore) — регенерация: `python3 src/fetch_sat.py`.
- **⚠️ ВЕРДИКТ СПУТНИКОВОГО ТЕСТА ПЕРЕВЁРНУТ наземной правдой пользователя**: пользователь
  указал реальную зону полёта (западнее дороги) — матчер лочился восточнее. Очный тест
  (`results/sat_faceoff.png`): белые точки + яркий квадратный объект, видимые камерой,
  ОТСУТСТВУЮТ на снимке (сцена изменилась после даты съёмки — временные объекты на площадке).
  Матчер залочил квадратный объект кадра на яркое пятно у дороги → **самосогласованное
  ЛОЖНОЕ семейство фиксов** (внутренняя точность 0.27–0.49 м, глобально сдвинуто на ~35 м).
  «42%/0.4 м» = точность ложной залочки, НЕ локализации. Уроки: (1) внутренняя
  согласованность ≠ правильность — ловится только внешней истиной (RTK в полётном плане!);
  (2) устаревший снимок над ИЗМЕНИВШЕЙСЯ сценой = опаснейший failure mode: уверенный
  систематический ложный fix, EKF поверит; (3) один доминирующий якорь (ховер над одним
  местом) = уязвимость — нужен консенсус фиксов с РАЗНЫХ участков маршрута; (4) для
  меняющихся поверхностей свой свежий ортофотоплан >> спутник; стабильные якоря
  (дороги/здания/перекрёстки) остаются валидными. Митигции для системы: фикс принимать
  только при согласии ≥2 разнесённых якорей, date-check подложки, гейт по расхождению
  с одометрией на длинной базе.
- **Эскалация против изменившейся сцены (всё отрицательно, зато определённо)**: после
  вскрытия ложной залочки проверены усиления: (1) якорная калибровка со стартом
  пользователя — все ветки тянутся к ложному якорю; (2) **георегистрация мозаики всего
  полёта** (63×58 м контекста, скан θ 0–355°, маскированный zero-centered NCC) — лучший
  NCC 0.112 = шум, углы разбросаны. Вывод: сцена изменилась настолько, что корреляционного
  соответствия с этим снимком НЕ СУЩЕСТВУЕТ ни на каком охвате/разрешении с 13 м. Полное
  разрешение кадра не лечит (матчинг работает на GSD спутника; провал контентный, не
  пиксельный). Пути: свежий ортофотоплан; крошки (иммунны по построению); на 50–100 м
  футпринт включает стабильные структуры (дороги/здания) — шанс есть, но только с
  консенсус-гейтом; серьёзный апгрейд — матчинг по инвариантным структурам (скелет дорог/
  границы, edge-based), игнорирующий изменчивое поле.
- **Мозаика-в-полёте (in-flight ortho) как подложка возврата — ВАЛИДИРОВАНА на обоих багах**:
  идея пользователя = наша GeoMap.paste. Карта из первых 60% полёта, локализация возврата:
  **баг-1 65%/0.41 м/p90 2.1; баг-2 49%/0.27 м/p90 0.67**. Иммунна к изменению сцены по
  построению (подложке минуты). Ключевое свойство: глобальный дрейф мозаики НЕ мешает
  возврату — дом лежит В мозаике, навигация к его карт-позиции = навигация к истинному дому;
  важна только локальная консистентность (одометрия 0.2 м/20 м). Память: коридор 2 км×60 м
  @0.15 м/px ≈ 5 МБ — тривиально. Апгрейды: localize-before-paste = мини-loop-closure
  (дрейф-коррекция мозаики, лёгкий SLAM); разовая георегистрация свежей мозаики к спутнику
  по стабильной структуре (дорога) → вся миссия наследует WGS84 при любых изменениях полей.
- **🔴 КОРНЕВАЯ ПРИЧИНА НАЙДЕНА (наводка пользователя): /odometry/image — ДЕБАГ-топик с
  нарисованными оверлеями!** Жёлтые точки-маркеры фич и служебная графика РИСУЮТСЯ их VO
  поверх кадра. Вся наша работа по багам шла на загрязнённых кадрах: «белые объекты на
  земле» = краска. Следствия: (1) спутниковый матчинг лочился на оверлеи → ложная привязка
  и «сцена изменилась» — диагноз неполный; (2) «объекты» в наших картах-в-полёте — оверлеи.
  Правильный топик: **/cam_usb/image/compressed — чистый, 640×480 JPEG, ~105 Гц** (в 13 раз
  быстрее дебага!). На чистых кадрах: одометрия чуть хуже (resid 1.61 vs 1.28, valid 79% —
  оверлейные точки были, по иронии, отличными фичами; голый грунт малоконтрастен), НО
  спутниковая привязка сменилась радикально: θ=+116° (тот поворот, что указал пользователь),
  петля легла север-юг вдоль тёмной полосы (39% фиксов, медиана консистентности 3.5 м).
  Кэш: bag1_cam_cache.npz. Уроки: проверять СОДЕРЖИМОЕ топика, а не название; дебаг-графика
  в кадрах = систематическая порча всех vision-слоёв; наземная правда пользователя дважды
  спасла от уверенных ложных выводов.
